# Local `npm run dev` only. GitHub Pages builds ignore this.
# Create a key at https://cursor.com/dashboard/integrations (or Dashboard → API Keys)
# then copy this file to `.env` and paste the key.

CURSOR_API_KEY=
CURSOR_MODEL=composer-2.5
